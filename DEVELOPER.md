# 개발 규칙

## 코드 컨벤션

- 변경 범위는 작게 유지하고 한 번에 한 실행 영역만 다룹니다.
- `apps/web`의 UI 코드는 TypeScript로 작성합니다.
- `apps/api`에서는 시선 처리, 상태 관리, Serial 연동을 Python으로 작성합니다.
- `firmware`에서는 장치 제어를 Arduino C++로 작성합니다.
- 짧은 약어보다 읽기 쉬운 이름을 우선합니다.
- web, API, firmware 전반에서 공통 명령 문자열은 동일하게 유지합니다.
- 여러 런타임에서 함께 쓰는 값은 `configs/default.json`에 둡니다.
- MVP에 꼭 필요하거나 복잡도를 낮추는 경우가 아니면 새 의존성은 추가하지 않습니다.

## Git 작업 규칙

- 작업마다 전용 feature 브랜치를 사용합니다.
- 커밋은 scaffold, web, API, firmware, docs처럼 논리적인 단위로 작게 나눕니다.
- 가능하면 컨벤셔널 커밋 형식을 사용합니다.
  - `chore:` 문서, 설정, 정리 작업
  - `feat:` 새 기능이나 화면
  - `fix:` 버그 수정
- 커밋 전에 해당 범위에서 가장 좁은 검증을 먼저 실행합니다.
  - 웹 변경: `apps/web`에서 `npm run build`
  - API 변경: Python 문법 또는 테스트 중심의 좁은 검증
  - 펌웨어 변경: 툴체인이 있으면 컴파일 확인
- push 전에 작업 트리를 깨끗하게 유지합니다.
- 사용자가 이어서 작업을 요청하면, 다음 큰 단계를 시작하기 전에 feature 브랜치를 원격에 푸시합니다.

## 점검 목록

- 변경 범위가 하나의 관심사 안에 머무르는가?
- 공통 명령 문자열이 모든 계층에서 일치하는가?
- 새 파일이나 의존성이 MVP에 정말 필요한가?
- 변경한 범위에 맞는 검증을 실행했는가?
